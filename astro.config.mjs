import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://RpidBing.github.io',
    // 添加buildOptions配置项
    buildOptions: "utf-8",
});
